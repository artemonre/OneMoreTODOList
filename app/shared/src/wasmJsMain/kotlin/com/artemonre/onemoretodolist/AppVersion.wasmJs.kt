package com.artemonre.onemoretodolist

// No runtime version metadata is generated for the web target yet - keep this in sync by hand
// with app/desktopApp/build.gradle.kts's nativeDistributions.packageVersion until the web app
// gets its own version source.
actual fun appVersionName(): String = "1.0.0"
